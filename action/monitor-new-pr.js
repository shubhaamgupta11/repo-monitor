// monitor-new-pr.js
async function monitorPRs(gitToken, slackToken, slackChannel, owner, repo, slackIDType, slackID) {
    // Logic here (reuse the script you've written, with args replaced by parameters)
  }
  
  module.exports = { monitorPRs };
  